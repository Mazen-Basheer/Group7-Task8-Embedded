# STM32 Bare-Metal ADC Monitor

A register-level C implementation for the **STM32F401CC** microcontroller that monitors battery voltage using its onboard Analog-to-Digital Converter (ADC). Built entirely from scratch without standard HAL or SPL libraries, utilizing direct register manipulation
